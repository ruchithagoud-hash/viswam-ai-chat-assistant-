streamlit>=1.20
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4  # or faiss (your platform may need different package)
transformers>=4.30.0
torch>=1.12.0
pandas
tqdm
python-dotenv
openai    # optional, only if you use OpenAI fallback
